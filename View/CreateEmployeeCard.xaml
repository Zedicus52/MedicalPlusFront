<UserControl x:Class="MedicalPlusFront.View.CreateEmployeeCard"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:assist="clr-namespace:MedicalPlusFront.Utils"
             xmlns:viewmodel="clr-namespace:MedicalPlusFront.ViewModel"  d:DataContext="{d:DesignInstance Type=viewmodel:CreateEmployeeVM}"
             xmlns:local="clr-namespace:MedicalPlusFront.View"
             mc:Ignorable="d" 
             d:DesignHeight="400" d:DesignWidth="830">
    <Grid>
        <StackPanel Orientation="Vertical"  HorizontalAlignment="Center" VerticalAlignment="Center">
            <Border Background="#B7D5E0" BorderBrush="#575176" CornerRadius="4" BorderThickness="1"  DockPanel.Dock="Top"  Height="480" Width="230">
                <StackPanel Orientation="Vertical" Margin="5">
                    <TextBlock Style="{StaticResource IdCreateUser}"/>
                    <Grid>
                        <TextBox Text="{Binding SurnameEmployeeInput, UpdateSourceTrigger=PropertyChanged}" x:Name="SurnameEmployeeText" Template="{StaticResource DefaultTextBox}" Height="25" Width="180"/>
                        <TextBlock Text="Прізвище" Style="{StaticResource SurnameEmployee}"/>
                    </Grid>
                    <Grid>
                        <TextBox Text="{Binding NameEmployeeInput, UpdateSourceTrigger=PropertyChanged}" x:Name="NameEmployeeText" Template="{StaticResource DefaultTextBox}" Height="25" Width="180"/>
                        <TextBlock Text="Ім'я" Style="{StaticResource NameEmployee}"/>
                    </Grid>
                    <Grid>
                        <TextBox Text="{Binding PatronymicEmployeeInput, UpdateSourceTrigger=PropertyChanged}" x:Name="PatronymicEmployeeText" Template="{StaticResource DefaultTextBox}" Height="25" Width="180"/>
                        <TextBlock Text="По батькові" Style="{StaticResource PatronymicEmployee}"/>
                    </Grid>
                    <Grid>
                        <TextBox Text="{Binding RoleEmployeeInput, UpdateSourceTrigger=PropertyChanged}" x:Name="RoleEmployeeText" Template="{StaticResource DefaultTextBox}" Height="25" Width="180"/>
                        <TextBlock Text="Роль" Style="{StaticResource RoleEmployee}"/>
                    </Grid>
                    <Grid Margin="0, 5, 0, 0">
                        <TextBox Text="{Binding LoginEmployeeInput, UpdateSourceTrigger=PropertyChanged}" x:Name="LoginEmployeeText" Template="{StaticResource DefaultTextBox}" Height="25" Width="180"/>
                        <TextBlock Text="Логін" Style="{StaticResource LoginEmployee}"/>
                    </Grid>
                    <StackPanel Orientation="Vertical">
                        <PasswordBox assist:PasswordBoxAssistant.BindPassword="True" 
                                 x:Name="PasswordEmployeeText" Template="{StaticResource DefaultPasswordBox}" 
                                 Height="25" Width="180" PasswordChanged="PasswordText_PasswordChanged">
                        </PasswordBox>
                        <TextBlock x:Name="PasswordPlaceHolder" Text="Пароль" Style="{StaticResource PasswordPlaceHolderEmp}"/>
                        <TextBlock TextWrapping="Wrap" x:Name="PasswordErrorText" Style="{StaticResource PasswordErrorTextWithout}"></TextBlock>
                    </StackPanel>
                    <Grid>
                        <Border Style="{StaticResource GenderBorder}">
                            <StackPanel Orientation="Horizontal" Width="100">
                                <Button Click="ButtonSelect_Click" Style="{StaticResource GenderBtn}">
                                    <Image Source="/View/Icons/man.png"></Image>
                                </Button>
                                <Border Background="#575176" Height="43" Width="1"/>
                                <Button Click="ButtonSelect_Click" Style="{StaticResource GenderBtn}">
                                    <Image Source="/View/Icons/woman.png"></Image>
                                </Button>
                            </StackPanel>
                        </Border>
                    </Grid>

                    <Button x:Name="SaveEmployeeBtn" Command="{Binding SaveEmployeeCommand}" Content="Зберігти" Template="{StaticResource DefaultButton}" Style="{StaticResource LoginButton}" Height="25" Width="100" FontSize="13" HorizontalAlignment="Left" Margin="20, 30, 0, 0"/>

                </StackPanel>
            </Border>
        </StackPanel>
    </Grid>
</UserControl>
